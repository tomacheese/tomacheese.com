import { defineContentConfig } from '@nuxt/content'

export default defineContentConfig({
  // This configuration is for @nuxt/content v3
  // Using minimal configuration since the default structure works well
  // The `collections` property is intentionally left empty because the default behavior is sufficient.
  // If custom collections are needed in the future, they can be defined here.
  // Examples of future custom configurations might include:
  // - Custom field validation
  // - Specific collection schemas
  // - Custom transformers or parsers
  collections: {}
})